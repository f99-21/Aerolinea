@model Vuelo
<h2>Editar Vuelo</h2>
<form method="post">
    <input type="hidden" name="IdVuelo" value="@Model.IdVuelo" />
    <label>Número de vuelo:</label>
    <input type="number" name="NumeroVuelo" value="@Model.NumeroVuelo" required />
    <label>Origen:</label>
    <input type="text" name="Origen" value="@Model.Origen" required />
    <label>Destino:</label>
    <input type="text" name="Destino" value="@Model.Destino" required />
    <label>Escala:</label>
    <input type="text" name="Escala" value="@Model.Escala" />
    <label>Precio:</label>
    <input type="number" step="0.01" name="Precio" value="@Model.Precio" required />
    <label>Horario:</label>
    <input type="datetime-local" name="Horario" value="@Model.Horario.ToString("yyyy-MM-ddTHH:mm")" required />
    <button type="submit">Guardar</button>
</form>